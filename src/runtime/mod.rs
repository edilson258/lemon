#![allow(dead_code)]
pub struct Runtime {}
// ir - execute compute time instructions, and inline instr
impl Runtime {
	pub fn init() {
		todo!("runtime initializing");
	}

	pub fn shutdown(self) {
		todo!("runtime shutting down");
	}
}
