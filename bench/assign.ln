

fn main() = {
  let age = 10;
  let age_ref = &mut age; // ref of mut age
  *age_ref = 18;

  //
  age_ref = &18; // error: cannot assign immutable 'age'

  // let age_ref = & age;
  // if (age < 18) {
  //   age_ref = &18;
  // }
}
