
fn change_age_ptr(age_ref: &mut &mut i32) = {
  *age_ref = &mut 20; // age now is 20
}

fn main() = {
  let mut age = 10;
  let mut age_ref = &mut age; // ref of mut age
  change_age_ptr(&mut age_ref);
}
