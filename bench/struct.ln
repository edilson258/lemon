type Person = {
  age: i32,
  born: i64,
}

impl Person = {
  fn new(age: i32, born: i64): Person = {
    return Person { age, born };
  }

  fn get_age(&self): i64 = {
    return self.born;
  }
}

fn main() = {}
