type Person = {
  age: i8,
}

impl Person = {
  fn new(age: i8): Person = {
    return Person { age };
  }
}



fn main() = {
  let person = Person::new(10);
  person.ag = 20;
//   println(person.age);
}
