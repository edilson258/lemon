type Person = {
  age: i8
}

impl Person = {
  fn new(age: i8): Person = {
    return Person { age };
  }
}



fn main() = {
  let mut person = Person::new(10);
  person.age = 20;
  println(person.age);
}
