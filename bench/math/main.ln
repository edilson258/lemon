let max = import("std");
let min = import("min.ln");

extern fn printf(fmt: str, ...): i32 = {};
fn println(value: i32) = {
  printf("%d\n", value);
}

fn main() = {
  let value = max(10, 20);
  println(value);

  let value = min(10, 20);
  println(value);
}
