pub const max = import("max.ln");

// const max = fn(a: i32, b: i32): i32  = {
//   if (a > b) return a;
//   return b;
// };

// pub fn check_max(left: i32, right: i32, expected: i32) = {
//   let test = max(left, right);
// }
