extern fn printf(fmt: str, ...): i32 = {};

fn println(value: i32) = {
  printf("%d\n", value);
}

fn max(a: i32, b: i32): i32 = if (a > b) { a } else { b };

fn main() = {
    let number = max(69, 420);
    println(number);
}
